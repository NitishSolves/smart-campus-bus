# Production build defaults.
# Set VITE_API_URL to your deployed backend origin + /api (no trailing slash).
# When unset, the app falls back to same-origin /api requests.
# Example: VITE_API_URL=https://your-backend.example.com/api
