# Copy to frontend/.env.local for local overrides (optional).
# Leave unset in development to use the Vite dev proxy (see vite.config.js).
# Production example:
# VITE_API_URL=https://your-backend.example.com/api
